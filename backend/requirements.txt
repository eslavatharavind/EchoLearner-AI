# ============================================================
# EchoLearn AI backend dependencies
# Pinned to the known-good versions that run locally, trimmed to
# only what the backend actually imports, and configured to install
# CPU-only PyTorch so it fits within Render's disk/memory limits.
# ============================================================

# ============ Web Framework ============
fastapi==0.128.6
uvicorn==0.40.0
python-dotenv==1.2.1
pydantic==2.12.5
python-multipart==0.0.22   # Required by FastAPI for Form/File uploads
httpx==0.28.1

# ============ Text Splitting (only langchain piece the code imports) ============
langchain-text-splitters==1.1.0

# ============ Vector Database ============
faiss-cpu==1.13.2
numpy==2.3.5

# ============ Document Processing ============
PyMuPDF==1.26.7            # Provides `fitz` (imported by pdf_loader.py)
pdfplumber==0.11.9         # PDF text-extraction fallback
Pillow==12.1.0             # Used by the scanned-PDF OCR rendering path
nbformat==5.10.4           # Jupyter Notebook parsing

# ============ Embeddings ============
# Install the CPU build of torch (much smaller than the default CUDA wheel).
# `torch==2.10.0` matches `2.10.0+cpu` from the index below, which pip prefers.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.10.0
transformers==5.1.0
sentence-transformers==5.2.2

# ============ Speech-to-Text ============
faster-whisper==1.2.1      # Uses CTranslate2 (no torch/CUDA needed)

# ============ Text-to-Speech ============
openai==2.18.0             # OpenAI client, also used for Groq (OpenAI-compatible)
gTTS==2.5.4                # Google TTS — default free provider
pydub==0.25.1              # Only used by the optional Coqui TTS path

# ============ Utilities ============
tqdm==4.67.3
requests==2.32.5
